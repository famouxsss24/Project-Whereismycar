# 사용자 관점 기능 구현 개요

이 문서는 Python `function()` 목록이 아니라, 사용자가 실제로 보는 프로그램 기능이 내부에서 어떻게 구현되는지 설명합니다.

## 기본 실행 흐름

| 사용자 기능 | 구현 방식 |
| --- | --- |
| 프로그램 시작 | 사용자는 `python main.py`와 이미지 경로 또는 웹캠 옵션을 입력합니다. 프로그램은 설정을 읽고 옵션을 검증한 뒤, 검출/OCR 파이프라인을 만들고 실행 모드를 선택합니다. |
| 설정 파일 사용 | 실행 옵션은 `settings.json`에 저장됩니다. 시작 시 이 파일을 읽어 기본값으로 사용하며, 사용자가 명령행에서 입력한 옵션이 있으면 그 값이 우선됩니다. |
| 정지 이미지 처리 | 프로그램은 디스크에서 이미지를 읽고, 주차 구역으로 나눈 뒤, 각 구역에서 번호판 후보를 검출하고 OCR로 번호판 문자를 읽어 결과를 출력하거나 발행합니다. |
| 웹캠 프레임 1회 처리 | 선택된 카메라를 열고 프레임 한 장을 캡처한 뒤, 동일한 검출/OCR 처리를 수행하고 결과를 발행한 후 카메라를 해제합니다. |
| 연속 실행 | loop 모드에서는 카메라를 계속 열어 둔 상태로 프레임 캡처, 검출, OCR, 결과 발행, 대기를 설정된 간격마다 반복합니다. |
| 실시간 미리보기 | preview 모드에서는 카메라 화면, 주차 구역 box, 번호판 box, 상태 텍스트, crop debug 화면을 OpenCV 창으로 보여줍니다. |

## 카메라 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| 카메라 하나 선택 | 사용자가 `--camera 0`처럼 카메라 번호 하나를 입력하면, 프로그램이 OpenCV로 해당 인덱스를 열고 해상도와 버퍼 크기를 설정합니다. |
| 여러 카메라 선택 | 사용자는 `--camera`를 여러 번 입력하거나 `--cameras 0,1`을 사용할 수 있습니다. 프로그램은 각 카메라를 열고 스트림별로 별도 처리합니다. |
| 카메라 해상도 설정 | 설정 파일에서 카메라별 width와 height를 지정할 수 있습니다. 지정하지 않으면 프로그램이 선호하는 고해상도 목록을 순서대로 시도하고 OpenCV가 받아들이는 가장 좋은 값을 사용합니다. |
| 카메라별 스캔 영역 사용 | `camera_section_boxes`에 카메라별 주차 구역 box를 저장할 수 있어, 서로 다른 카메라 각도에 맞는 인식 영역을 사용할 수 있습니다. |

## 주차 영역 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| 자동 주차 구역 분할 | 사용자 지정 box가 없으면 프로그램이 설정된 구역 수와 layout에 따라 이미지를 같은 크기의 열 또는 행으로 나눕니다. |
| 사용자 지정 인식 영역 | 사용자는 `--section-box x,y,width,height`를 입력하거나 설정 파일에 box를 저장할 수 있습니다. 이 box들은 자동 구역 분할을 대체합니다. |
| 이름 있는 주차 구역 | 구역은 `a`, `b`, `c` 같은 소문자 이름을 가집니다. 이 이름은 출력에서 parking zone과 section name으로 사용됩니다. |
| 구역 이름 중복 방지 | 여러 카메라나 저장된 box가 같은 이름을 사용하면, 프로그램이 뒤쪽 중복 이름을 다시 지정해 발행되는 zone 이름이 고유하게 유지되도록 합니다. |
| 미리보기에서 스캔 영역 저장 | preview 모드에서 사용자가 그린 box는 `settings.json`에 다시 저장되어 다음 실행 때 같은 영역이 복원됩니다. |

## 번호판 검출 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| 검출 방식 선택 | 사용자는 `heuristic` 또는 `yolo`를 선택합니다. 프로그램은 처리 시작 전에 선택된 방식에 맞는 검출기를 생성합니다. |
| 휴리스틱 번호판 검출 | 프로그램은 대비 처리, gradient, thresholding, contour filtering 같은 이미지 처리 단계를 사용해 번호판 모양의 영역을 찾습니다. |
| YOLO 번호판 검출 | 프로그램은 학습된 YOLO 모델을 로드하고 object detection을 실행한 뒤, confidence가 높은 box를 선택해 번호판 crop을 추출합니다. |
| YOLO fallback | YOLO를 사용하더라도 fallback이 허용된 경우, YOLO가 놓친 구역은 휴리스틱 검출기로 다시 확인합니다. |
| 회전된 번호판 보정 | 검출된 번호판 crop이 기울어져 있으면 프로그램이 가능한 범위에서 crop을 곧게 펴 OCR이 읽기 쉬운 이미지로 만듭니다. |
| OCR 입력 품질 향상 | 번호판 crop에 grayscale 변환, 대비 향상, 확대, padding을 적용한 뒤 OCR을 다시 시도합니다. |

## OCR 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| 한국 번호판 읽기 | OCR 엔진이 번호판 crop에서 텍스트 조각을 추출하고, 프로그램이 그 조각들을 조합해 한국 번호판 형식에 맞는 문자열 후보를 만듭니다. |
| 잘못된 번호판 텍스트 거부 | OCR 결과가 예상 한국 번호판 패턴과 맞는지 확인한 뒤, 맞는 경우에만 유효한 번호판으로 처리합니다. |
| 가장 좋은 OCR 결과 선택 | 프로그램은 원본 crop의 OCR 결과와 향상된 crop의 OCR 결과를 비교해, 유효하고 confidence가 더 높은 결과를 사용합니다. |
| 반복 OCR 작업 감소 | 같은 이미지 crop은 내용 기반 cache를 사용해 다시 OCR하지 않도록 처리합니다. |

## 결과 출력 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| JSON 결과 출력 | 처리된 프레임은 source, timestamp, detector, section, occupied 여부, plate text, confidence, box 정보를 포함한 JSON payload로 변환됩니다. |
| 빈 구역과 점유 구역 표시 | 각 구역 결과에는 번호판 후보가 검출되었는지, 유효한 번호판을 읽었는지, 비어 있는지 여부가 기록됩니다. |
| 번호판 이미지 crop 포함 | 결과 발행 시 preview와 이미지 업로드에 사용할 수 있도록 구역 crop과 보정된 번호판 crop을 메모리에 보관합니다. |
| 중복 업데이트 억제 | 같은 번호판은 설정된 cooldown 시간이 지나기 전까지 반복 발행하지 않습니다. |

## 결과 발행 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| HTTP 서버로 결과 전송 | server URL이 설정되어 있으면 유효한 번호판 업데이트를 JSON으로 POST합니다. |
| 번호판 이미지 로컬 저장 | 로컬 이미지 발행이 활성화되어 있으면 crop된 번호판 이미지를 로컬 폴더에 저장하고, 내장 static server로 접근할 수 있게 합니다. |
| 번호판 이미지 Azure 업로드 | Azure Blob Storage가 설정되어 있으면 번호판 이미지를 JPEG로 인코딩해 지정된 container에 업로드하고 blob URL을 반환합니다. |
| 임시 Azure 이미지 링크 사용 | SAS 만료 시간이 설정되어 있으면 업로드된 Azure 이미지 URL에 임시 read-only SAS token을 붙입니다. |
| Firebase record 업데이트 | Firebase가 설정되어 있으면 검출된 번호판을 설정된 database path 아래에 zone, 마지막 네 자리, image URL, entry time과 함께 저장합니다. |
| 최초 진입 시간 보존 | Firebase에 이미 entry time이 있는 번호판은 이후 업데이트에서도 기존 entry time을 덮어쓰지 않습니다. |

## 미리보기 조작 기능

| 사용자 기능 | 구현 방식 |
| --- | --- |
| 스캔 영역 그리기 | 사용자가 preview 창에서 마우스로 드래그하면, 프로그램이 마우스 좌표를 프레임 안의 pixel box로 변환합니다. |
| 선택 영역 이름 변경 | 소문자 키를 누르면 선택된 스캔 영역에 해당 section name이 지정됩니다. |
| 선택 영역 전환 | `Tab` 키를 누르면 사용 가능한 스캔 영역을 순서대로 선택합니다. |
| 스캔 영역 초기화 | reset 동작은 설정된 영역 또는 기본 영역을 복원하고 processor에도 반영합니다. |
| 스캔 영역 삭제 | clear 동작은 활성 스캔 영역을 제거하고 변경된 설정을 저장합니다. |
| crop debug 보기 | 두 번째 창에서 구역 crop과 번호판 crop을 보여주어 OCR이 성공하거나 실패하는 이유를 확인할 수 있게 합니다. |

## 유지보수 유틸리티

| 사용자 기능 | 구현 방식 |
| --- | --- |
| Firebase 데이터 확인 | `check_db.py`가 Firebase service account로 접속해 저장된 database record를 출력합니다. |
| Azure 이미지 링크 갱신 | `refresh_firebase_blob_sas.py`가 Firebase에 저장된 이미지 URL을 읽고 만료된 Azure SAS link를 새 read-only link로 교체합니다. |
| 업로드된 번호판 이미지 크기 조정 | `resize_uploaded_plate_images.py`가 번호판 이미지 blob을 다운로드하고 resize/re-encode한 뒤 blob을 덮어쓰고 reference를 업데이트합니다. |
