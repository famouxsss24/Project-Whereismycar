# 간단 구현 개요

이 문서는 사용자가 보는 기능을 기준으로 프로그램이 어떻게 동작하는지 짧게 설명합니다.

## 프로그램이 하는 일

이 프로그램은 주차 공간의 차량 번호판을 인식합니다. 저장된 이미지나 실시간 카메라 영상을 사용할 수 있습니다.

## 실행 방식

1. 사용자가 `python main.py`로 프로그램을 실행합니다.
2. 프로그램은 `settings.json`과 명령행 옵션을 읽습니다.
3. 이미지 모드, 웹캠 모드, 반복 모드, 미리보기 모드 중 하나를 선택합니다.
4. 각 프레임을 처리하고 JSON 결과를 만듭니다.

## 카메라 또는 이미지 입력

- 이미지 모드에서는 이미지 파일 하나를 읽습니다.
- 웹캠 모드에서는 선택된 카메라 번호를 엽니다.
- 여러 카메라를 설정하면 각 카메라 영상을 따로 처리합니다.
- 카메라 해상도는 `settings.json`에서 설정할 수 있습니다.

## 주차 공간

- 프로그램은 이미지를 여러 주차 구역으로 나눕니다.
- 구역은 자동으로 행 또는 열로 나눌 수 있습니다.
- 사용자가 직접 스캔 영역을 그리거나 설정할 수도 있습니다.
- 각 구역은 `a`, `b`, `c` 같은 이름을 가집니다.

## 번호판 검출

- 프로그램은 각 주차 구역 안에서 번호판 위치를 찾습니다.
- YOLO 모델을 사용해 학습 기반 검출을 할 수 있습니다.
- 휴리스틱 이미지 처리 방식도 사용할 수 있습니다.
- YOLO가 번호판을 놓치면 휴리스틱 방식으로 다시 확인할 수 있습니다.

## OCR

- 번호판이 검출되면 해당 부분을 crop합니다.
- crop 이미지를 정리하고 향상합니다.
- OCR이 번호판 문자를 읽습니다.
- 프로그램은 읽은 문자가 한국 번호판 형식에 맞는지 확인합니다.

## 결과

프로그램은 다음과 같은 정보를 출력합니다.

- 카메라 또는 이미지 source
- 주차 구역 이름
- 구역 점유 여부
- 검출된 번호판 문자
- confidence 점수
- 번호판과 구역의 box 위치

## 미리보기 모드

미리보기 모드에서는 실시간 카메라 창을 보여줍니다. 사용자는 다음을 할 수 있습니다.

- 주차 구역 box 확인
- 검출된 번호판 box 확인
- 마우스로 스캔 영역 그리기
- 키보드 문자로 스캔 영역 이름 변경
- crop/debug 이미지 확인

## 결과 발행

프로그램은 결과를 여러 방식으로 발행할 수 있습니다.

- JSON 출력
- HTTP 서버로 JSON 전송
- crop된 번호판 이미지 로컬 저장
- Azure Blob Storage에 번호판 이미지 업로드
- Firebase Realtime Database 업데이트

## 유틸리티 스크립트

추가 스크립트는 유지보수를 돕습니다.

- `check_db.py`는 Firebase 데이터를 확인합니다.
- `refresh_firebase_blob_sas.py`는 Azure 이미지 링크를 갱신합니다.
- `resize_uploaded_plate_images.py`는 업로드된 번호판 이미지 크기를 조정합니다.
